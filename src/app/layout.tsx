import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "next-themes";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tania's Cybersecurity Services - Phishing Awareness Analysis",
  description: "Secure Yourself With Tania. Interactive phishing awareness analysis tool to identify, analyze, and defend against phishing attacks.",
  keywords: ["Phishing", "Cyber Security", "Tania's Cybersecurity Services", "Phishing Awareness", "Email Analysis", "Security"],
  authors: [{ name: "Tania's Cybersecurity Services" }],
  icons: {
    icon: "/tanias-logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem={false}
          disableTransitionOnChange={false}
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
